import styles from './styles.module.css';
import MainContainer from '../../components/mainContainer';

function Profile() {
  return (
    <MainContainer>
      <div className={styles.profile_container}>Profile</div>
    </MainContainer>
  );
}

export default Profile;
